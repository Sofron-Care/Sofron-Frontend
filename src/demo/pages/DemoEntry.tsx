import DemoNav from "../components/DemoNav";

export default function DemoEntry() {
  return (
    <div style={{ minHeight: "100vh", background: "#FFFFFF" }}>
      <DemoNav />
      {/* Future search + business info goes here */}
    </div>
  );
}
